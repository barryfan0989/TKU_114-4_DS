# 程式設計與資料結構期末實作考試

總分：120 分。共 12 題，每題 10 分，全部都要完成。

## 繳交結構

在 repository 建立 `final_exam` 資料夾，只放下列 12 個 Java 檔案：

```text
final_exam/
├── Q01_PriorityRecord.java
├── Q02_MinHeapInsert.java
├── Q03_MinHeapRemove.java
├── Q04_ChainedHashTable.java
├── Q05_StudentHashIndex.java
├── Q06_AdjacencyMatrixGraph.java
├── Q07_AdjacencyListGraph.java
├── Q08_BfsTraversal.java
├── Q09_DfsPathSearch.java
├── Q10_UnweightedShortestPath.java
├── Q11_BstHashDirectory.java
└── Q12_CampusDispatchSystem.java
```

每個 `public class` 必須與檔名一致。需要的 helper class、record 或 enum 寫在同一檔案中，不要增加其他繳交檔案。

---

## 第 1 題：PriorityQueue 多欄位排序

指定檔名與類別：`Q01_PriorityRecord.java`

```java
public record Job(String id, int priority, long sequence) {}
public static java.util.List<String> processOrder(java.util.List<Job> jobs)
```

規則：忽略 `null` job；priority 數字越小越優先；相同 priority 依 sequence 小到大；仍相同依 id 字典序。輸入 `null` 或 empty 時回傳新的 empty List，不得修改輸入 List。

配分：Comparator 4 分、PriorityQueue 3 分、null/empty 2 分、結果獨立性 1 分。

## 第 2 題：Min Heap Insert

指定檔名與類別：`Q02_MinHeapInsert.java`

```java
public void add(int value)
public Integer peek()
public int size()
public java.util.List<Integer> snapshot()
public boolean isValidMinHeap()
```

使用自行管理的 `ArrayList<Integer>`，不可使用 `PriorityQueue` 或排序。`add` 必須使用 bubble-up；重複值允許；空 Heap 的 `peek()` 回傳 `null`；snapshot 不可暴露內部 List。

配分：add/bubble-up 5 分、peek/size 2 分、snapshot 1 分、invariant 2 分。

## 第 3 題：Min Heap Remove

指定檔名與類別：`Q03_MinHeapRemove.java`

```java
public Q03_MinHeapRemove(java.util.List<Integer> values)
public Integer removeMin()
public Integer peek()
public int size()
public java.util.List<Integer> snapshot()
```

Constructor 必須 bottom-up heapify，忽略輸入中的 `null`；`removeMin` 使用最後元素補 root 並 bubble-down；空 Heap 的 `removeMin()` 與 `peek()` 回傳 `null`；不可使用 `PriorityQueue` 或 sort。

配分：heapify 3 分、remove/bubble-down 4 分、empty/single 2 分、snapshot 1 分。

## 第 4 題：Separate Chaining Hash Table

指定檔名與類別：`Q04_ChainedHashTable.java`

```java
public Q04_ChainedHashTable(int bucketCount)
public void put(int key, String value)
public String get(int key)
public boolean remove(int key)
public int size()
public int longestChain()
```

Bucket 使用 List 實作 separate chaining；相同 key 更新 value 且 size 不增加；不同 collision key 都要保留；支援負 key；bucketCount 小於等於 0 時丟出 `IllegalArgumentException`。

配分：index/collision 3 分、put/update 3 分、get/remove 2 分、size/longest 2 分。

## 第 5 題：HashMap 與 HashSet 選課索引

指定檔名與類別：`Q05_StudentHashIndex.java`

```java
public boolean enroll(String studentId, String courseId)
public boolean drop(String studentId, String courseId)
public java.util.Set<String> coursesOf(String studentId)
public java.util.Set<String> studentsIn(String courseId)
public int enrollmentCount()
```

輸入要 trim 且轉大寫；null/blank 操作回傳 false；重複選課回傳 false；drop 最後一門課後移除空 key；查詢回傳不可修改的獨立 Set。

配分：雙向索引 4 分、enroll/drop 3 分、normalization 2 分、結果保護 1 分。

## 第 6 題：Adjacency Matrix

指定檔名與類別：`Q06_AdjacencyMatrixGraph.java`

```java
public Q06_AdjacencyMatrixGraph(java.util.List<String> vertices)
public boolean addEdge(String first, String second)
public boolean removeEdge(String first, String second)
public boolean hasEdge(String first, String second)
public int degree(String vertex)
public java.util.List<String> neighbors(String vertex)
```

建立 undirected Graph；拒絕 self-loop、missing vertex 與重複 edge；vertex 順序依 constructor 輸入；neighbors 依 vertex 原始順序；查詢 missing vertex 回傳安全結果，不得發生 index error。

配分：matrix/undirected 4 分、add/remove 2 分、degree/neighbors 3 分、edge cases 1 分。

## 第 7 題：Directed Adjacency List

指定檔名與類別：`Q07_AdjacencyListGraph.java`

```java
public boolean addVertex(String vertex)
public boolean addEdge(String from, String to)
public boolean removeEdge(String from, String to)
public java.util.List<String> outgoing(String vertex)
public int inDegree(String vertex)
public int edgeCount()
```

使用 Map 與 Set 建立 directed Graph；拒絕 missing vertex、self-loop 與重複 edge；outgoing 保留 edge 加入順序；missing vertex 回傳 empty List 或 0。

配分：vertex/edge 4 分、directed semantics 2 分、degree/count 2 分、edge cases 2 分。

## 第 8 題：BFS Traversal

指定檔名與類別：`Q08_BfsTraversal.java`

```java
public static java.util.List<String> bfs(
        java.util.Map<String, java.util.List<String>> graph, String start)
public static java.util.Map<String, Integer> distanceFrom(
        java.util.Map<String, java.util.List<String>> graph, String start)
```

使用 Queue 與 visited；順序依 adjacency List；distance 中 start 為 0；不可到達 vertex 不放入 Map；graph/start 無效時回傳新的 empty collection；不可修改輸入 Graph。

配分：BFS order 4 分、distance 3 分、cycle/visited 2 分、invalid input 1 分。

## 第 9 題：DFS 與 Reachability

指定檔名與類別：`Q09_DfsPathSearch.java`

```java
public static java.util.List<String> dfs(
        java.util.Map<String, java.util.List<String>> graph, String start)
public static boolean reachable(
        java.util.Map<String, java.util.List<String>> graph,
        String start, String target)
```

`dfs` 必須使用 recursion；使用 visited 防止 cycle；順序依 adjacency List；start 與 target 都存在且相同時 reachable 為 true；missing vertex 回傳 false。

配分：recursive DFS 4 分、reachable 3 分、cycle 2 分、edge cases 1 分。

## 第 10 題：無權重最短路徑

指定檔名與類別：`Q10_UnweightedShortestPath.java`

```java
public static java.util.List<String> shortestPath(
        java.util.Map<String, java.util.List<String>> graph,
        String start, String target)
```

必須使用 BFS 與 predecessor；回傳包含 start、target 的完整路徑；多條最短路徑時依 adjacency List 順序；不可到達或 missing vertex 回傳 empty List；start 等於 target 回傳單一元素。

配分：BFS 3 分、predecessor 3 分、path reconstruction 2 分、edge cases 2 分。

## 第 11 題：BST 與 HashMap 雙索引

指定檔名與類別：`Q11_BstHashDirectory.java`

```java
public boolean add(int id, String name)
public String findName(int id)
public boolean remove(int id)
public java.util.List<Integer> idsBetween(int low, int high)
public int size()
```

BST 依 id 排序，HashMap 依 id 直接找 name；id 必須大於 0，name 必須 trim 後非空；重複 id 回傳 false；新增與刪除後兩份索引必須一致；range 結果遞增；low 大於 high 回傳 empty List。

配分：雙索引一致性 3 分、BST add/find 2 分、remove 3 分、range/size 2 分。

## 第 12 題：校園服務派送系統

指定檔名與類別：`Q12_CampusDispatchSystem.java`

```java
public record Request(String id, String location, int priority, long sequence) {}
public boolean addLocation(String location)
public boolean addRoad(String first, String second)
public boolean submit(Request request)
public Request nextReachable(String serviceCenter)
public java.util.List<String> route(String start, String target)
public int pendingCount()
```

使用 adjacency list 保存 undirected roads、HashMap 防止重複 request id、PriorityQueue 依 priority 小到大與 sequence 小到大排序。`nextReachable` 取出目前可由 serviceCenter 到達的最高優先 request；不可到達 request 必須保留。`route` 使用 BFS 最短路徑。無效輸入回傳安全結果。

配分：Graph/route 3 分、HashMap uniqueness 2 分、PriorityQueue 3 分、unreachable/edge cases 2 分。

## 完成要求

1. 12 個檔案都必須能以 JDK 17 編譯。
2. 不得修改指定 class、record、constructor 或 method 名稱。
3. 不得以固定輸出取代資料結構操作。
4. 所有回傳 collection 都不得暴露可修改的內部狀態。
5. 將 `final_exam` 資料夾的 GitHub 連結繳交至 iClass。

